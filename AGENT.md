# AGENT.md

## 作用和文档边界

本文件只规定代码协作、架构边界和交付质量，不复制产品需求、技术方案或接口契约中的业务事实。

实现任务前，按需阅读以下文档：

- 产品范围、用户流程和业务语义：`docs/product/01-知识体系平台产品需求文档（PRD）.md`
- 架构、数据模型、任务、部署和实施顺序：`docs/backend/01-知识体系平台后端技术方案.md`
- API 路径、字段、枚举、响应和错误码：`docs/backend/02-知识体系平台接口契约文档.md`

文档职责固定如下：

- 产品文档维护“做什么”和“为什么做”;
- 技术方案维护“如何实现”和系统边界;
- 接口契约维护 API 的可执行约定;
- `AGENT.md` 维护“如何修改代码和交付”。

不要把业务范围、完整接口清单、表字段清单、枚举值或产品流程复制到本文件。它们变更时，先更新对应文档，再同步代码、Schema、迁移和测试。文档、代码或用户最新决策冲突时，暂停扩大修改范围，先恢复一致。

## 最小修改原则

- 优先修改已有实现，先查找调用方、复用点和当前职责边界。
- 不重写整个模块，不创建第二套逻辑，不创建重复组件。
- 不为未来场景增加抽象、依赖、模块、配置或空接口。
- 优先复用已有 Components、Hooks、Services、Repository、Utils 和 Types。
- 当前仓库没有某类抽象时，不为了满足名称而新建平行层；沿用现有模块边界，只有技术方案明确要求时才引入。
- 修复根因和共享入口，避免在每个调用方重复加补丁。
- 保留用户已有的未相关修改，不使用破坏性 Git 命令覆盖工作区。

## 架构一致性

- 不绕过已有状态机、Repository、Service 或统一 API。
- API 层只处理 HTTP 参数、鉴权依赖、调用 Service 和统一响应；业务规则、事务边界、状态迁移和权限复核放在现有 Service 边界。
- Repository 存在时，数据访问通过 Repository；不存在时，不为一次任务强行创建第二套 Repository 层。
- Job、脚本和 API 复用同一套 Service，不复制导入、校验、发布或回滚逻辑。
- 跨模块调用使用已有公开 Service 或明确查询边界，不直接改写其他模块的内部状态。
- 数据库约束、事务、行锁、鉴权、审计和错误处理属于正确性要求，不得为了减少代码而省略。
- 持有事务或行锁时不得调用远端 HTTP、对象存储或其他慢外部服务。
- 数据库结构变更必须有 Alembic migration；不得用启动时 `create_all` 代替生产迁移。

## 变更流程

1. 阅读本文件及任务相关的产品、技术和接口文档。
2. 检查当前实现、调用方、测试和工作区状态，确认任务边界。
3. 在已有职责边界内完成最小修改；优先复用已有代码。
4. 非平凡逻辑补一个能失败的最小测试，尤其是状态、事务、鉴权、锁和解析逻辑。
5. API 变更同步 Schema、接口契约/OpenAPI 和调用方；数据库变更同步模型、迁移和集成测试。
6. 执行质量门禁，检查钩子是否修改文件，并在交付中说明测试结果和未执行项。

## Python 代码质量硬门禁

所有新增或修改的 Python 代码必须满足：

- `ruff check .` 无 lint 错误；
- 使用 `ruff format .` 统一格式，并通过 `ruff format --check .`；
- 通过仓库配置的 pre-commit 提交钩子；
- pre-commit 校验失败，或钩子修改了文件时，不得提交；
- 每个 Python 文件最多 500 行，行数包含空行和注释。

交付前必须执行：

```bash
ruff format .
ruff check .
ruff format --check .
pre-commit run --all-files
find app migrations scripts tests -name '*.py' -print0 | xargs -0 wc -l
```

发现 Python 文件超过 500 行时，必须按现有职责边界拆分后再交付。不得通过压缩代码、合并语句、删除必要注释或排除文件来规避限制。若仓库没有某个被统计的目录，先确认目录是否属于当前项目，再如实记录检查结果，不要伪造目录或绕过统计。

## 测试和交付

- 至少运行与修改范围相关的测试；涉及公共 API、迁移、鉴权、事务或发布逻辑时运行完整测试集。
- 不只断言 HTTP 200；同时断言响应字段、错误码、状态变化和数据持久化结果。
- 需要 MySQL 事务、锁、唯一约束或迁移验证时，使用 MySQL 集成环境，不以 SQLite 结果替代。
- 不提交 Secret、签名、数据库密码、临时文件、缓存、构建产物或生成文件。
- 交付说明必须包含：修改文件、验证命令及结果、未执行项、已知兼容影响。
- 除非用户明确要求，不自动提交、推送或创建 Pull Request。

## 禁止事项

- 不把产品决策、技术方案正文或接口契约正文塞进 `AGENT.md`。
- 不以单个调用方、样例数据或前端隐藏按钮代替业务规则和后端授权。
- 不绕过统一响应、统一错误处理、状态机、事务或审计链。
- 不让开放接口读取草稿或修改正式数据。
- 不删除失败测试、放宽约束或修改公共契约来“通过检查”。
- 不恢复已废弃的技术栈或架构，除非最新技术方案明确批准。
